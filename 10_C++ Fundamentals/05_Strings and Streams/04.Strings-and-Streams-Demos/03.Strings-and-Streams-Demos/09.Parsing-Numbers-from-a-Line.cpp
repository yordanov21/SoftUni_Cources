#include <iostream>
#include <string>
#include <sstream>
#include <vector>
using namespace std;

int main () {
    string line;
    stoi
    //read the input 
    istringstream lineStream(line);
    vector<int> numbers;
    vector<string> charackters;

    for (size_t i = 0; i < line.size(); i++)
    {
        if (stoi(line[i]))
        {
            int currentNum = stoi(inline[i]);
            numbers.push_back(currentNum)
               
        }
        else
        {
            charackters.push_back(line[i])
        }
    }

    return 0;
}
