{{!-- <section id="notifications">
        <div class="p-3 mb-2 bg-success w-50 text-white text-center" id="successNotification">Success</div>
        <div class="p-3 mb-2 bg-danger w-50 text-white text-center" id="errorNotification">Error</div>
        <div class="p-3 mb-2 bg-info w-50 text-white text-center" id="loadingNotification">Loading...</div>
    </section> --}}

<!-- Navigation -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container">
                       {{!-- добавяме пътя в href-a --}}
        <a class="navbar-brand" href="#/home">DonUni</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive"
            aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">

                {{!-- //вкарваме ги в тази if/else конструкция и спрямо булевата променлива isLoggedIn ще се визуализират --}}
                {{#if isLoggedIn}}
                <li class="nav-item">
                              {{!-- добавяме пътя в href-a за dashboard--}}
                    <a class="nav-link" href="#/cause/dashboard">$$$$$$$$$$$$$$$$$$$</a>
                </li>
                <li class="nav-item">
                           {{!-- добавяме пътя в href-a за logout--}}
                    <a class="nav-link" href="#/user/logout">Logout ({{ username }})</a>
                </li>
                {{else}}
                <li class="nav-item">
                {{!-- добавяме пътя в href-a --}}
                    <a class="nav-link" href="#/user/login">Login</a>
                </li>
                {{/if}}

            </ul>
        </div>
    </div>
</nav>
<!-- ./Navigation -->